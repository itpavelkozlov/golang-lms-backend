service:

  logger:
    cores:
      - file
      - stdout
#      - stderr
    filename: tests/lmg.log
    maxSize: 1024
    maxBackups: 5
    compress: true
    level: debug

  database:
    dbname: postgres
    username: admin
    password: admin
    host: localhost
    port: 5432